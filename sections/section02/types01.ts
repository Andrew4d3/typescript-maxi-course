// Some common types

// Boolean!
let isDone: boolean = false;

// There is no need to explicitly indicate the type  since TS is able to infer it
let isReady = false;

// Numbers
let decimal: number = 6;
let hex: number = 0xf00d;
let binary: number = 0b1010;
let octal: number = 0o744;

// Strings
let color: string = "blue";
color = "red";

console.log(color);
