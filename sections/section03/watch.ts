// So far we've had to re-enter the compiler command every time we make a change
// We can avoid doing that, if we use the --watch flag like this:
//  tsc app.ts --watch

const myName = "hello6";
console.log(myName);
